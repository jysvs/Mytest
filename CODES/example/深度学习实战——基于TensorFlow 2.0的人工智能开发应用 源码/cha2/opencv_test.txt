# 导入opencv包，Python版OpenCV
import cv2

def read_image(image_path):
    """读取图像
	参数:
		image_path：图像所在路径
	返回:
		矩阵数据格式，维度，矩阵数据
	"""
    img = cv2.imread(image_path)
    return type(img), img.shape, img 

def writeImage(sourceDir, objectDir):
    """保存图像
	参数：
		sourceDir：原始图像路径
		objectDir：目标保存路径
	返回：
		无
	"""
	# 读取图像
	img = cv2.imread(sourceDir)
	# 保存图像
	cv2.imwrite(objectDir, img)

if __name__ == "__main__":
    # 图像路径
    image_path = "./images/python3.6.png"
    # 读取图像，生成图像矩阵数据
    img_type, img_shape, img_matrix = read_image(image_path)
    # 输出图像矩阵数据格式
    print("Type of imread function processed image: {}".format(img_type))
    # 输出图像尺寸
    print("Shape of imread funciton processed image: {}".format(img_shape))
    # 输出图像矩阵数据
    print("Imread function processed image: {}".format(img_matrix))
    # 原始图像路径
	sourceDir = "./images/python3.6.png"
	# 保存图像路径
	objectDir = "./images/python3.6saved.png"
	# 保存图像
	writeImage(sourceDir, objectDir)

